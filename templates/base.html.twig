<!DOCTYPE html>
<html lang="fr">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<link rel="icon" type="image/png" href="{{ asset('favicon.png') }}">
		<script src="https://cdn.tailwindcss.com"></script>
		<script src="https://cdn.jsdelivr.net/npm/feather-icons/dist/feather.min.js"></script>

		<title>
			{% block title %}Factures
			{% endblock %}
		</title>
		<script src="https://cdn.tailwindcss.com"></script>
	</head>
	<body
		class="bg-gray-100 text-gray-800">

		<!-- Barre de navigation -->
		<nav class="bg-indigo-600 text-white shadow">
			<div class="max-w-6xl mx-auto px-4 py-3 flex justify-between items-center">
				<a href="{{ path('facture_index') }}" class="text-lg font-bold tracking-wide">
					💼 Gestion des Factures
				</a>
				<div class="space-x-4">
					<a href="{{ path('facture_index') }}" class="hover:underline">Factures</a>
					<a href="{{ path('facture_new') }}" class="hover:underline">➕</a>
					<a href="{{ path('api_facturx_ui') }}" class="hover:underline bg-indigo-500 px-3 py-1 rounded text-sm">API PDF</a>
				</div>
			</div>
		</nav>

		<!-- Contenu principal -->
		<main class="max-w-6xl mx-auto px-4 py-6"> {% block body %}{% endblock %}
			</main>

			<!-- Footer -->
			<footer class="bg-gray-200 text-center py-4 text-sm text-gray-600">
				©
				{{ "now"|date("Y") }}
				- Gestion des factures avec Symfony & FacturX - Bastien with ❤️
			</footer>
		</body>
	</html>
